# NextRides.com – Car Classifieds Platform

NextRides.com – это современный веб‑сервис объявлений о продаже автомобилей в США, разработанный как полнофункциональное приложение (React + FastAPI + MongoDB).

Проект создавался как реальный продукт, а не учебный пример: с продуманным поиском, профилями пользователей, чатом покупатель–продавец и удобной мобильной версией.

---

## Основная идея

NextRides.com помогает быстро найти «следующую машину» по реальным параметрам: марка, модель, год, пробег, цена, расположение и статус Clean Title. 

Платформа ориентирована на частных продавцов и небольшие дилерские центры, которые хотят:
- размещать объявления с качественными фотографиями;
- общаться с покупателями прямо внутри сайта;
- управлять своими объявлениями, избранным и сохранёнными поисками.

Покупателям же важно:
- удобно искать по живым фильтрам;
- видеть статусы, пробег, CT и цену сразу в карточках;
- быстро связываться с продавцом (звонок или сообщения).

---

## Основные возможности

### 1. Пользователи и профили

- Регистрация и вход по email + пароль (JWT‑аутентификация).
- Публичный профиль:
  - аватар,
  - имя пользователя,
  - список опубликованных объявлений.
- Страница профиля владельца объявления доступна из нескольких мест:
  - кнопка на детальной странице авто;
  - аватар в карточке объявления;
  - участник чата.

### 2. Объявления о продаже авто

Каждое объявление содержит:
- минимум **3 фотографии** (ограничение размера: до 1 МБ на изображение);
- марка и модель (только из готового списка для единообразия);
- год, пробег, цена;
- тип привода (FWD / RWD / AWD / 4WD);
- город (выбор из списка городов США) и ZIP (строго 5‑значный US ZIP);
- телефон продавца (валидация формата);
- VIN;
- детальное описание (30–1000 символов);
- статус **Clean Title (Yes/No)** – визуально подсвечивается на карточках и в деталях.

Особенности:
- CT‑значок (зелёный кружок с «CT») отображается:
  - на детальной странице рядом с заголовком,
  - на карточке – в нижнем правом углу фотографии.
- Превью карточки:
  - первая строка: `Марка Модель` + цена;
  - вторая строка: пробег и город;
  - аватар продавца в углу картинки, переход к профилю по клику.

### 3. Поиск и фильтры

Главный сценарий – быстрый поиск «снизу экрана»:

- Фиксированная кнопка **“Go Search!”** на всех страницах.
  - При нажатии открывается панель фильтров.
  - Кнопка меняется на **“Hide Search!”**, рядом всегда доступна зелёная **“Show Matches”**.
  - На мобильных панель можно закрыть свайпом вниз.

Фильтры включают:
- Марку (select);
- Модель (зависит от выбранной марки, только из словаря моделей);
- Диапазон года, пробега и цены;
- Тип привода;
- Статус Clean Title (Any / Clean Title Only);
- ZIP и расстояние.

Результаты поиска отображаются в виде сетки карточек (до 16 превью на главной), CT и количество лайков видны прямо в списке.

### 4. Избранное и лайки

- Любой авторизованный пользователь может добавить объявление в избранное.
- Сердце избранного и счётчик лайков объединены:
  - в карточке и на детальной странице:
    - красное сердце, если объявление в избранном у текущего пользователя;
    - под иконкой – количество добавлений в избранное (лайков), с форматированием `1.2K` при значениях > 1000.

Это даёт быстрый визуальный индикатор интереса к конкретному авто.

### 5. Сообщения и чат

Система сообщений реализована как чат «покупатель – продавец» по каждому объявлению.

- Отправка сообщений из детальной страницы объявления.
- Страница **Messages**:
  - сверху – аккуратное меню выбора чата:
    - список всех диалогов (независимо от того, кто написал первым);
    - у каждого чата:
      - аватар собеседника,
      - имя,
      - заголовок объявления,
      - микро‑превью автомобиля,
      - индикатор непрочитанных сообщений.
    - элементы разделены рамками и мягкими полупрозрачными фонами разных оттенков (emerald, blue, amber, pink) для визуального различения.
  - под меню – окно чата:
    - пузырьки сообщений слева/справа (я / собеседник),
    - временная метка,
    - поле для ввода и кнопка отправки.

История переписки подгружается по выбранному чату, новые сообщения сразу появляются в ленте.

### 6. Профиль и настройки

- Загрузка аватара пользователя (с серверной обработкой и хранением файлов).
- Управление личными данными.
- Просмотр списка собственных объявлений (**My Listings**) и создание/редактирование:
  - загрузка фотографий (минимум 3, максимум по размеру 1 МБ каждая),
  - удаление старых фотографий при редактировании с синхронизацией с сервером.

---

## Технический стек

### Frontend
- **React** (SPA)
- **React Router** – маршрутизация между страницами (Home, Car Detail, Profile, Messages и др.)
- **Axios** – общение с API
- **TailwindCSS / utility‑классы** – стили и адаптивная вёрстка
- **shadcn/ui Button** – унифицированные стили кнопок

### Backend
- **FastAPI** – API‑сервер
- **MongoDB** – хранилище данных
  - коллекции: `users`, `listings`, `favorites`, `saved_searches`, `messages`
- **JWT** – авторизация
- Встроенная раздача загруженных изображений через `/api/images/...`

### Архитектура
- Фронтенд и бэкенд разделены, общаются по REST.
- Все URL API начинаются с `/api` (важно для прокси и ingress).
- Изображения хранятся на сервере и доступны по проксируемым URL.

---

## Старт проекта (локально)

> Ниже – базовый пример, адаптируйте под свою среду.

### Backend

```bash
cd backend
python -m venv venv
source venv/bin/activate  # Windows: venv\\Scripts\\activate
pip install -r requirements.txt

# убедитесь, что в backend/.env заданы MONGO_URL и DB_NAME
uvicorn server:app --reload --port 8001
```

### Frontend

```bash
cd frontend
yarn install
yarn start
```

Фронтенд ожидает бэкенд по адресу из `REACT_APP_BACKEND_URL` в `frontend/.env`.

---

## Возможные направления развития

- Расширенный список городов/ZIP‑кодов с автодополнением.
- Уведомления о новых сообщениях (web‑push или email).
- Более продвинутый рейтинг продавцов и история сделок.
- Фильтры по дополнительным параметрам (тип кузова, двигатель, цвет и т.п.).

---

## Назначение этого репозитория

В этом репозитории находится полный исходный код NextRides.com:
- фронтенд (React) в папке `frontend/`;
- бэкенд (FastAPI) в папке `backend/`;
- вспомогательные файлы конфигурации и документация.

Репозиторий можно использовать как:
- основу для собственного сервиса объявлений;
- демонстрационный проект для портфолио;
- стартовую точку для изучения стека **React + FastAPI + MongoDB**.

Если вы просматриваете этот проект на GitHub – добро пожаловать в NextRides.com 🚗